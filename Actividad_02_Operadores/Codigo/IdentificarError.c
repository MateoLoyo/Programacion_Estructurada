#include <stdio.h>

int main()
{
    int x=5, y=3;
    if(x>y)
    {
        //falta esta llave de abrir.
        printf("x es mayor");//identar esta parte
    }//falta esta llave de cerrar.
    else
    {
        //falta esta llave de abrir.
        printf("y es mayor");//identar esta parte
    }//falta esta llave de cerrar.
    printf("\nfin");//al poner la llave anterior el fin se tiene que sacar y falta el salto de linea
    return 0;
}
